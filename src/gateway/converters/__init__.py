"""Converters: per-source-type input → canonical markdown + YAML frontmatter.

Real converter implementations land in M2 (web) and M8 (youtube/arxiv/pubmed) per BUILD.md.
The `_search/` subpackage holds API helpers copied from research-notebook.
"""
