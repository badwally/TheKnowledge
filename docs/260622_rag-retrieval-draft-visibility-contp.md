# Continuation prompt — RAG draft-visibility fix (paste into a fresh session after /clear)

```
Objective: implement the Hole 1 fix for `wiki retrieve` — stop hard-excluding draft pages so the curated concept/entity/synthesis layer is reachable by the default agent retrieval path — without degrading citation fidelity. Critical project: this brings the knowledge system to high utility and is the prerequisite for any later embedding/hybrid-retrieval work.

Read first (full context lives here; do not re-derive):
- docs/260622_rag-retrieval-draft-visibility-brief.md   ← the diagnosis, pinned mechanism, fix options, open design questions, and the probe goldens (Appendix A) + triage script (Appendix B)
- docs/260623_rag-draft-visibility-adversarial-read.md  ← adversarial read of THIS handoff (findings 1–8). Findings 1–5 are folded into the sections below; 6–8 live only in the note. Read it before validating.
- src/gateway/ops/retrieve.py  (lines 160, 282 — include_drafts default)
- src/gateway/search_index.py  (lines 356, 385-386 — the WHERE p.draft=0 filter; 461 — _authority_key / _DRAFT_PENALTY)

Pinned root cause: retrieve() defaults include_drafts=False → search_fts adds `WHERE p.draft = 0` → drafts removed from the candidate pool BEFORE ranking. ~30% of concepts, ~30% of entities, 92% of synthesis are perma-draft (legacy migration), so ~1,100 curated pages are invisible to `wiki retrieve`. `wiki search` (include_drafts=True) ranks the same pages #1 — that divergence is the whole bug.

Next atomic step: settle three OPEN design questions (in the brief) WITH the user — do not self-decide — then implement the chosen demote-not-exclude variant.
  (1) A alone vs A+D: demote drafts via the existing _DRAFT_PENALTY vs ALSO content-gate out `_(needs population...)_` placeholder sections. NOTE (finding 4, sharpened by Appendix C): A alone is NOT stub-safe — the empty-section skip (retrieve.py:195) drops only *empty* text; the placeholder string is non-empty, so A-alone WILL surface it. And D must gate at SECTION level (drop the placeholder sections, KEEP the lede + Methods) — NOT page level, because the probe's positive targets ARE hybrid pages whose value is the lede. A+D-at-section-level is more load-bearing than the brief's original recommendation implies — weigh Q1 with that in view.
  (2) Annotate draft provenance on the <page> block tag (draft="true") so consumers know it's unfinalized? (author leans yes — still open)
  (3) Persist the probe goldens as a permanent fixture .knowledge/eval/retrieval/semantic_mismatch.yaml? Cheap. BUT "wire into the gate" is NOT cheap and is partly a non-sequitur as written (finding 3): eval-retrieval scores the FTS layer, not the retrieve assembly the fix touches. Gating the actual fix means promoting the Appendix-B retrieve-path scorer into the eval harness — real work; scope it explicitly, don't assume "persist the yaml" gates the fix. (author leans yes on persist — gate-wiring still open)
Then: implement demote-not-exclude in retrieve.py/search_index.py. MECHANICS NOTE (finding 5): option A *requires* flipping include_drafts→True on the retrieve path (the only way drafts enter the candidate pool) and THEN relying on _DRAFT_PENALTY (+ optional content-gate) to demote them. "Do not blind-flip B" does NOT mean "avoid touching include_drafts" — it means flip it AND verify stubs stay down. Persist the goldens fixture, and validate.

Validate (the eval harness only tests the FTS layer, NOT the retrieve assembly — so the Appendix-B script is the only instrument that exercises the ACTUAL fix):
- PRECONDITION (finding 2) before trusting any before/after delta: confirm every `expect:` slug in the goldens resolves to a REAL page (`wiki search` / file check). Hand-built goldens may name non-existent slugs (the verbose synthesis-style ones are the risk); a bad slug reads MISS before AND after, silently masking the real signal. Fix or drop any non-resolving golden first.
- ⛔ HARD GATE — STUB POLLUTION (finding 1, made concrete in brief Appendix C; do NOT declare done without it): the legacy drafts are HYBRID — a real lede + Methods, then `## Summary`/`## Key claims`/`## Sources`/`## Related` each a literal `_(needs population from legacy import)_` (177 such pages; verified on mesolimbic-dopamine-system-modulation, apple-silicon-and-neural-engines). The empty-section skip (retrieve.py:195) does NOT drop these — the placeholder is non-empty. So A-alone WILL return `_(needs population from legacy import)_` as grounding context. REQUIRED INVARIANT: run every probe query through `wiki retrieve` and grep the assembled blocks for `needs population from legacy import` — the count MUST be 0 (script in Appendix C). Any positive count = the fix is floating stubs = NOT done. This is the inert-in-production trap the playbook exists to catch; a green that only measures recall is the false green. Also assert the verified-finalized controls (active-vs-passive-qt, automated-lockbox, 30-year-cash-flow-projection) still HIT (no displacement). EVIDENCE-BEFORE-DONE: paste the G-POS before/after, the G-NEG-1 grep count = 0, and the G-NEG-2 finalized results before claiming complete.
- before/after: run the Appendix-B triage script (scores the real `wiki retrieve --json` path). Current ~2/21; expect the 3 controls to flip to HIT. The "~0.33 FTS ceiling" is a SOFT reference, not a pass bar (finding 7) — it was measured on `wiki search` (tiered/bm25); post-fix retrieve uses _authority_key + _DRAFT_PENALTY (different ranking), so a correct fix may land below 0.33. Judge on controls-flip + good-drafts-surface + stubs-stay-down, not on hitting 0.33.
- regression: `.venv/bin/wiki eval-retrieval --compare --k 10` must not regress recall@10 ≥ 0.90 on the existing easy goldens. (Caveat: runs on lexically-easy goldens; will NOT catch paraphrased stub-pollution — that is what the negative control is for.)
- pre-merge gate: `.venv/bin/python -m gateway.scripts.gate`.

Constraints / gotchas (un-carried, these get re-broken):
- This is a citation-grounding BEHAVIOR change → keep the design pass; do NOT blind-flip include_drafts=True (that's blunt option B). RESUME the paused brainstorming flow on the open design questions and get user sign-off BEFORE implementing — Q1 (A alone vs A+D) is a genuine design fork, and forks with multiple valid approaches are consult-before-implement here. All three questions (Q1/Q2/Q3 in the brief) are deliberately left OPEN for this session to settle with the user; do not self-decide.
- ISOLATION (already set up for you): the work happens in a git worktree so other concurrent `~/code/knowledge` sessions are not disturbed. The worktree + branch ALREADY EXIST: **`/Users/andrewgrant/code/knowledge-wt-rag-draft-visibility`** on branch **`fix/rag-draft-visibility`** (cut from main `305b4136`, pushed to origin), with the three handoff docs (brief, this contp, adversarial-read) ALREADY COMMITTED on it. `cd` into that worktree and work there. Do NOT create a second worktree or branch, and do NOT continue the merged+deleted `fix/authorship-loop-comprehensive`. Leave the unrelated `knowledge-wt-youtube-filter` worktree untouched.
- VENV-IN-WORKTREE (load-bearing gotcha, FIRST thing to do): the worktree has NO `.venv` yet, and the main checkout's `.venv` editable-installs `gateway` from MAIN's src — so using it would test the wrong code. In the worktree run `python3 -m venv .venv && .venv/bin/pip install -e '.[dev]'` before running anything. Then always use the worktree's `.venv/bin/python` / `.venv/bin/wiki`, never system python and never main's venv.
- Inside your worktree you OWN `docs/session-state.md` (no concurrent session shares your branch). Verify `git branch --show-current` == `fix/rag-draft-visibility` before ANY commit; never commit to main; merge back via push + PR (user merges). The handoff docs are already committed; your first NEW commit is the eval fixture + code. Recover any orphaned work via `git show <sha>:<path>`.
- No direct writes to wiki/ or raw/ (gateway-only hard rule). This work is src/gateway + docs + eval fixtures, which is fine.

Non-goals (stay scoped to Hole 1):
- Do NOT start Hole 2 (embedding / hybrid retrieval) — re-baseline the probe AFTER this fix first.
- Do NOT start Hole 3 (finalize/cull the ~1,100 perma-draft backlog) — parallel curation track, separate.
- NotebookLM is orthogonal; leave it.
```

Left out deliberately: the embedding-model option comparison (EmbeddingGemma/Qwen3/Voyage) and the full three-hole sequencing — both live in the brief, and both belong to Hole 2, which is out of scope for the next session.
