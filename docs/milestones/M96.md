# M96 — Phase 8 Round A (schema debt clearance — timestamp + sources_count backfills)

**Tag:** `m96-phase8-round-a`
**Branch:** `phase8-round-a` → merged to `main`
**Tests:** 1769 → 1777 (+8), 0 regressions
**Lint before:** schema-drift: 4597
**Lint after:** schema-drift: 210

## Deliverable

Ran `wiki backfill-timestamps` (existing op) and added + ran `wiki backfill-sources-count` (new op). Combined: 4597 → 210 schema-drift findings, all auto-fixable items cleared. Remaining 210 are structural editorial issues requiring human judgment (invalid `entity_kind` values, missing required sections, long slugs, missing `canonical_name`).

## What ran

- `wiki backfill-timestamps --dry-run` → 2191 pages, 0 errors
- `wiki backfill-timestamps` → 2191 entity/concept/synthesis pages stamped with `created_at`/`last_updated` from file mtime
- Schema-drift: 4597 → 309

- `wiki backfill-sources-count --dry-run` → 99 pages, 0 errors  
- `wiki backfill-sources-count` → 99 synthesis pages stamped with `sources_count` (unique `[[sources/...]]` wikilink count)
- Schema-drift: 309 → 210

## New op: `src/gateway/ops/backfill_sources_count.py`

- `backfill_sources_count(dry_run=False) -> OperationResult`
- Walks `wiki/synthesis/` via `PAGE_SCHEMAS`
- Skips pages that already have `sources_count`
- Counts unique `[[sources/...]]` wikilinks via regex
- Uses `write_atomic` for safe writes
- Added to CLI (`backfill-sources-count --dry-run`) and `CLI_ONLY` in mcp_server

## Remaining schema-drift (210, not auto-fixable)

| Error type | Count | Fix path |
|---|---|---|
| `wiki-page-section-missing` | 68 | Editorial: write required sections per entity |
| `entity-kind-unknown` | 58 | Editorial: reclassify to controlled vocabulary |
| `slug-too-long` | 50 | Editorial: rename + update backlinks |
| `wiki-page-required-field` | 34 | Editorial: add `canonical_name`/`entity_kind` |

## Tests

`tests/gateway/test_backfill_sources_count.py` — 8 tests covering stamp on missing field, unique-source counting, zero count, skip on existing, dry-run no-write, empty directory, CLI registration, `--dry-run` parse.
