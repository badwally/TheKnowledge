# M65 — Phase 4 Round A

**Date:** 2026-05-26  
**Branch:** phase4-round-a → main  
**Tag:** m65-phase4-round-a  
**Tests:** 1218 → 1240 (+22), 0 regressions

---

## Items delivered

### QUAL-9 — Cross-domain contamination quarantine in promote-domain

- `src/gateway/lint/domain_purity.py` (new): walks `wiki/sources/*.md`, reports pages tagged to multiple blessed domains (centroid drift). Blessed domain = directory under `.knowledge/policies/` with a `policy.yaml`.
- `src/gateway/ops/promote_domain.py`: added `_check_contamination(member_ids, proposed_domain) -> list[str]`. Checks raw source frontmatter for pre-existing domain tags that exclude the proposed domain. Writes `contamination_warnings: [ids]` to proposal frontmatter on promote — non-blocking, warns only.
- `src/gateway/ops/lint.py`: registered `domain_purity` check as `"domain-purity"`.
- `tests/gateway/test_qual9_domain_purity.py` (new): 11 tests — lint coverage (empty, single blessed, multi-blessed, non-blessed ignored) + contamination pre-flight (no outliers, foreign-tagged flagged, includes proposed not flagged, promote writes warnings).

### AGT-6 — Brief generator (per-domain nlm-briefing cron)

- `src/gateway/ops/briefing_cron.py` (new): `run_briefing_cron()` iterates blessed domains, computes corpus hash (sorted `stem:mtime_ns` of raw sources tagged to domain), skips if hash unchanged, calls `nlm_briefing(domain)`, stores updated hash at `.knowledge/briefing-cron/<domain>.json` via `write_atomic`.
- `src/gateway/cli.py`: added `briefing-cron` subcommand (no args).
- `src/gateway/mcp_server.py`: added `"briefing-cron"` to `CLI_ONLY` (agents must not trigger corpus-wide NLM runs; scheduler owns invocation).
- `.knowledge/schedule.yaml`: weekly entry `0 6 * * 1` (Monday 06:00 UTC), cooldown 3600s.
- Hard guard: `briefing_cron.py` contains no `nlm_audio` or `nlm_slides` references — verified by structural test.
- `tests/gateway/test_agt6_briefing_cron.py` (new): 11 tests — hash roundtrip, corpus hash determinism, run skips unchanged, run calls briefing on change, hash stored after success, error forwarded on NLM failure, structural guard for no audio/slides escalation.

### AGT-12 — `.claude/skills/wiki-*` skill family

Six skill files at `.claude/skills/<name>/SKILL.md`:

| Skill | Purpose |
|-------|---------|
| `wiki-ingest-triage` | Triage a URL/file for ingest — fit check, domain selection, `wiki ingest` |
| `wiki-finalize-drafts` | Surface stale drafts, classify Cat A/B/C, finalize or abandon |
| `wiki-research` | Structured research via `wiki research` + `wiki query` → synthesis page |
| `wiki-cite` | Add citations via `wiki cite-suggest` + `wiki cite-add` / `wiki cite` |
| `wiki-domain-bootstrap` | Bootstrap policy, seed concepts, verify filter readiness |
| `wiki-editorial-review` | Lint + read + rank findings; read-only until user confirms |

Each skill: references WIKI.md section anchors, uses `wiki <subcommand>` CLI, embeds no gateway-owned prompts.

---

## ARCH-14 interaction

`briefing_cron.py` reads `raw_dir()` for corpus hashing but writes only to `.knowledge/briefing-cron/` (internal, not wiki/raw). ARCH-14 test tripped on the `raw_dir()` + `.write_text()` combination. Fixed by using `write_atomic` for the hash store — correct regardless and consistent with the write-path invariant.

---

## Carry-forward

- ONT-11 backfill: 56/94 synthesis pages lack `synthesizes:` — lint warns. Escalate to error after backfill pass.
- ANTHROPIC_API_KEY_RESEARCH needed for condo-capital-infra and edge-ai-agentic eval runs.
- WIKI.md § 9 should document hard-rule-1 write invariant explicitly (deferred from ARCH-14).
