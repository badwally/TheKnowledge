# M91 — Phase 7 Round C (ONT-12 codify tags field)

**Tag:** `m91-phase7-round-c`
**Branch:** `phase7-round-c` → merged to `main`
**Tests:** 1716 → 1726 (+10), 0 regressions

## Deliverable

ONT-12: `tags:` field codified as optional `list[str]` for sub-domain topic
clustering. Decision: keep (149 concepts already use it with meaningful values);
document in WIKI.md; enforce type with validator warning and lint check.

## Implementation

**`src/gateway/validator.py`**
- `tags-invalid-type` WARNING if `tags:` present but not a list of strings

**`src/gateway/lint/invalid_tags.py`**
- `run()` walks all wiki pages; SEVERITY_WARNING for any page with a non-list
  or list-with-non-string-items `tags:` field; check slug `tags-invalid-type`

**`src/gateway/ops/lint.py`**
- Registered `("tags-invalid-type", invalid_tags.run)` in `_CHECKS`

**`WIKI.md`**
- Concept template updated with `tags:` field example and documentation
- Section note after concept schema explaining semantics (free-form kebab-case,
  sub-domain topic clustering, lint-enforced type)

**`tests/gateway/test_ont12_tags.py`** — 10 tests
- Validator: accepts valid list, empty list, absent; warns on string, non-str items
- Lint: passes for valid/absent tags, flags string tags, WARNING severity, known checks
