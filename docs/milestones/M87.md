# M87 — Phase 6 Round C (ONT-5 canonical_source on paper entities)

**Tag:** `m87-phase6-round-c`
**Branch:** `phase6-round-c` → merged to `main`
**Tests:** 1673 → 1684 (+11), 0 regressions

## Deliverable

ONT-5: paper entities (`entity_kind: paper`) must carry a `canonical_source:` field
pointing to an ingested raw source ID. Validator warns; lint surfaces all violations.

## Implementation

**`src/gateway/lint/paper_canonical_source.py`**
- `run()` walks wiki/entities/; flags `entity_kind: paper` pages without `canonical_source:`
- `SEVERITY_WARNING`; check slug `paper-canonical-source`

**`src/gateway/validator.py`**
- In `validate_wiki_page_frontmatter`: WARNING (`paper-missing-canonical-source`) if `entity_kind == "paper"` and no `canonical_source:`. Non-blocking (warning, not error) for finalize.

**`src/gateway/ops/lint.py`**
- Registered `paper-canonical-source` check

**`tests/gateway/test_ont5_paper_canonical_source.py`** — 11 tests
**`tests/gateway/test_lint.py`** — updated KNOWN_CHECKS set
