# M64 — Phase 3 Round C (ARCH-14, ONT-11, QUAL-13)

**Date:** 2026-05-26
**Branch:** `phase3-round-c`
**Tag:** `m64-phase3-round-c`
**Tests:** 1208 → 1218 (+10, 0 regressions)

---

## Deliverables

### ARCH-14: CI guard for hard rule #1 (no direct wiki/raw writes)

**Files changed:**
- `tests/gateway/test_arch14_hard_rule_1.py` (new) — 3 tests
- `src/gateway/ops/contradiction.py` — fix: 2 write_text → write_atomic (real violation caught)
- `src/gateway/ops/wiki_agenda.py` — fix: write_text → write_atomic

**Behavior:**
- `test_no_writes_in_read_only_zone[lint]` — asserts no lint/ file combines wiki/raw path construction with write calls
- `test_no_writes_in_read_only_zone[web]` — asserts no web/ file combines wiki/raw path construction with write calls
- `test_ops_write_path_uses_write_atomic` — asserts ops/ modules that construct wiki/raw paths use write_atomic not write_text
- **Found and fixed**: `ops/contradiction.py` was writing directly to `wiki/sources/<id>.md` and `wiki/contradictions/<slug>.md` via `.write_text()` instead of `write_atomic()` — a real hard rule #1 violation in the codebase since M?

**Note:** WIKI.md § 9 should document the invariant explicitly; deferred to next doc pass.

---

### ONT-11: `synthesizes:` coverage lint warning

**Files changed:**
- `src/gateway/lint/synthesizes_coverage.py` (new)
- `src/gateway/ops/lint.py` — registered `synthesizes-coverage` check
- `tests/gateway/test_ont11_synthesizes_coverage.py` (new) — 4 tests

**Behavior:**
- Walks `wiki/synthesis/*.md` and warns (`severity: warning`) when a page lacks `synthesizes:` frontmatter
- M45 established the `synthesizes:` mechanism; 38/94 synthesis pages currently have it; this check surfaces the 56 that don't
- Severity stays WARNING until a backfill pass covers existing pages (escalate to ERROR post-backfill per ONT-11 acceptance)

---

### QUAL-13: Wayback Machine snapshot at web ingest

**Files changed:**
- `src/gateway/converters/web.py` — `_wayback_snapshot()` adapter + wire-up in `convert()`
- `tests/gateway/test_converters.py` — 3 new tests (archive capture, omit-on-failure, continue-on-raise)

**Behavior:**
- After successful fetch+extract, web converter calls `https://web.archive.org/save/<url>` (POST, 15s timeout)
- On success: response URL stored in `meta.archive_url` of source frontmatter
- On failure/timeout: silently skips (no `archive_url` key emitted); ingest continues uninterrupted
- Monkeypatching target: `_wayback_snapshot(url) -> str | None`

---

## Bonus fix

- `ops/contradiction.py`: two pre-existing hard rule #1 violations (direct `.write_text()` to wiki paths) caught by ARCH-14 test and corrected to use `write_atomic`.

---

## Next

M65 — Phase 3 Round D. Remaining Phase 3 candidates:
- `QUAL-9` (cross-domain contamination quarantine in promote-domain)
- `AGT-4` (contradiction sweeper)
- `AGT-5` (domain steward)
- `INT-12` (wiki → Notion read-only mirror)
- `ONT-6` (enforce documented frontmatter: last_updated, created_at)
