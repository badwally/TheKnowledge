# M72 — Phase 5 Round E

**Date:** 2026-05-26  
**Branch:** phase5-round-e → main  
**Tag:** m72-phase5-round-e

## Deliverables

### ONT-13 — `last_verified_at` for time-sensitive entity kinds

Statutes and standards change — they are amended, revised, superseded. Entity pages for these kinds now carry a `last_verified_at` timestamp, and lint enforces it.

**Files added/modified:**

- `src/gateway/lint/stale_verified.py` — new lint check
- `src/gateway/ops/lint.py` — registered `stale-verified` check
- `tests/gateway/test_ont13_stale_verified.py` — 15 tests
- `tests/gateway/test_lint.py` — updated `KNOWN_CHECKS` expected set
- `WIKI.md` §4.1 — documented `last_verified_at` field; expanded `entity_kind` enum

**Lint contract:**

| Condition | Severity | Check |
|-----------|----------|-------|
| `statute`/`standard` missing `last_verified_at` | ERROR | `stale-verified` |
| `last_verified_at` present but >365 days old | WARNING | `stale-verified` |
| `last_verified_at` unparseable | ERROR | `stale-verified` |
| Any other `entity_kind` | (no finding) | — |

## Tests

1413 passing (15 new).
