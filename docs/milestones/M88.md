# M88 — Phase 6 Round D (ONT-7 per-claim confidence 3-tier GRADE)

**Tag:** `m88-phase6-round-d`
**Branch:** `phase6-round-d` → merged to `main`
**Tests:** 1684 → 1695 (+11), 0 regressions

## Deliverable

ONT-7: optional `confidence: established|tentative|speculative` field on any wiki page.
Validator rejects invalid tiers. Lint surfaces per-domain distribution and propagation
violations (synthesis stronger than cited source evidence).

## Implementation

**`src/gateway/lint/claim_confidence.py`**
- `run_distribution()`: per-domain count of established/tentative/speculative/none pages; INFO findings
- `run_propagation()`: synthesis pages whose confidence exceeds weakest cited source; WARNING findings
- `CONFIDENCE_TIERS`, `_TIER_RANK` constants

**`src/gateway/validator.py`**
- `confidence-invalid` ERROR if `confidence:` field not in `{established, tentative, speculative}`

**`src/gateway/ops/lint.py`**
- Registered `confidence-distribution` + `confidence-propagation` checks

**`tests/gateway/test_ont7_claim_confidence.py`** — 11 tests
**`tests/gateway/test_lint.py`** — updated KNOWN_CHECKS set

## Propagation rule

synthesis.confidence ≤ min(source.confidence for source in synthesizes)

Where tier order is: established < tentative < speculative (established is strongest evidence).
Synthesis citing only annotated-tentative sources should not claim established confidence.
