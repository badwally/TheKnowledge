# M102 — Phase 9 Round C (parse_iso consolidation — single canonical impl in gateway.core)

**Tag:** `549e879`
**Branch:** `main`
**Tests:** 1830 → 1837 (+7), 0 regressions

## Deliverable

Extracted `parse_iso()` to `gateway.core`, removing 6 private `_parse_iso` duplicates
across the codebase. The canonical implementation is a strict behavioral superset of all
removed variants.

## Implementation

**`src/gateway/core.py`** (modified)
- `parse_iso(value: object) -> datetime | None`: canonical ISO-8601 parser using `fromisoformat` with Z→+00:00 normalization, UTC fallback for naive datetimes, non-string/None guard, and dual ValueError/TypeError handling. Never raises.

**Removed from:**
- `src/gateway/scheduler.py` — replaced `strptime` variant with module-level `parse_iso` import
- `src/gateway/lint/stale_verified.py` — removed; added `from gateway.core import parse_iso`
- `src/gateway/lint/stale_drafts.py` — removed; added `from gateway.core import parse_iso`
- `src/gateway/ops/abandon_stale_drafts.py` — removed; updated `from gateway.core import OperationResult, parse_iso`
- `src/gateway/ops/wiki_digest.py` — removed multi-format strptime variant; added `from gateway.core import parse_iso`
- `src/gateway/ops/daily_review.py` — removed multi-format strptime variant; updated `from gateway.core import OperationResult, parse_iso`

**Not touched:** `gateway.research.query_plan_store._parse_iso_z` — intentionally different contract (non-optional, required Z suffix).

## Tests

`tests/gateway/test_core.py` — 7 new tests: UTC Z suffix, explicit offset, date-only string, None input, non-string input, empty string, invalid format.
