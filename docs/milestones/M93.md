# M93 — Phase 7 Round E (TOOL-10 shell completion + --help examples)

**Tag:** `m93-phase7-round-e`
**Branch:** `phase7-round-e` → merged to `main`
**Tests:** 1740 → 1740 (±0), 0 regressions

## Deliverable

TOOL-10: `argcomplete` was already wired and declared as a dependency.
Completed the acceptance criteria: documented the eval line in CLAUDE.md
and added `--help` epilog examples to key subcommands that were missing them.

## Implementation

**`CLAUDE.md`** (Operation guide section)
- Shell completion block: `eval "$(register-python-argcomplete wiki)"` with
  instructions to add to `~/.zshrc` / `~/.bashrc`

**`src/gateway/cli.py`** — epilog examples added to:
- `cite-capture`: quote + URL with optional --target
- `daily`: plain, with options, --json pipe
- `finalize`: normal, synthesis, --abandon
- `cite-add`: exact match, fuzzy match

Previously documented: `ingest`, `filter`, `status`, `evaluate`, `context`,
`query`, `research`, `lint` (8 subcommands already had examples).
